[package]
name = "xtask"
description = "The LokoOS build driver: builds the boot chain, assembles an ESP, and measures the size budget."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[[bin]]
name = "xtask"
path = "src/main.rs"

# No dependencies on purpose. The build driver is the one thing that has to
# work on a machine where nothing else is set up yet.
[dependencies]
